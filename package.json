{
  "dependencies": {
    "node-sass-chokidar": "^1.3.4"
  },
  "devDependencies": {},
  "scripts": {
    "production-build-client": "cd frontend && npm run production-build-client",
    "rails-start": "cd backend && rails s -p 3001",
    "heroku-deploy": "git subtree push --prefix backend heroku master"
  }
}
